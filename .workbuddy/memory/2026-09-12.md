# 2026-09-12

## 哔咔源代码评审（只读，未改源码）
- 范围：PicacgSource / PicaClient / PicaHttpApi / PicaHttpEngine / PicaModels / PicaApi / PicaException / BcTls / PicaConfig / Source 三件套 / SourcePrefs(哔咔部分) / SecureAccountStore，+ 集成点 PiKAApp、DownloadManager、HomeViewModel、ComicDetailViewModel、ReaderViewModel、RankScreen。禁漫源实现、UI 层、更新链路、构建脚本排除。
- 产出：`reports/bika-source-review-20260912.html`（17 条问题，P0×0 / P1×3 / P2×7 / P3×8，含修复建议+修复后效果+验证缺口+V1..V6）。
- 结论要点（待修，尚未落地）：
  - **F1（P1）** `ImageDetail.directUrl` 三分支存在交集漏洞：`fileServer` 已含 static 且 `path` 以 `static/` 开头 → 拼出 `static/static/` → 图片 404。
  - **F2（P1）** `PicaClient.safeCall` 统一要求 `data != null`，但 update*/sendComment/replyComment/forgotPassword 是纯确认型接口；服务端省略 data 时会「成功报失败」（发评论最危险，会重复提交）。建议拆 `safeCallUnit` + 抽出 `withRetry(block, requireData)`。
  - **F3（P1）** BcTls.install 失败只 Log.e，PicaClient.init 不重试（JmClient/UpdateManager 会重试，行为不一致）→ 整个哔咔源 100% 不可用且伪装成网络故障；PicaHttpEngine 每请求刷 warning。
  - **F4（P2）** PicaConfig 用 `const val` 分段拼接 → 编译期常量折叠 + R8 常量传播，产物里仍是单条完整明文密钥。注释声称的混淆在 release 下等于零，反而造成风险误判。（可用 `strings`/apktool 反查 release APK 直接证伪）
  - **F5（P2）** `Doc.comicId = thumb?.let { id ?: uid } ?: uid` 把「有无封面」当「数据来自哪个接口」的隐式开关 → 缺封面时导航 ID 静默切换。
  - **F6（P2）** 模型关键集合零默认值（`ignoreUnknownKeys` 不覆盖缺失字段；`coerceInputValues` 只对有默认值的属性生效）→ 一次脏响应整页解析失败。
  - **F7（P2）** `SourceManager.onUnauthorized()` 按全局 `_activeSource` 重登，不按 401 归属源 → 切源窗口串源（用 A 的凭据登 B）。
  - **F8（P2）** 凭据失效靠中英文文案 contains 判定，与项目「用结构化错误码」范式（PicaException.isRateLimit）矛盾 → 坏凭据永留，每次冷启动掉登录。
  - **F9（P2）** DownloadTask 未固化 SourceType，runTask 用 `SourceManager.current()` → 下载中切源串源。修法：任务加 `source: String = "PICACG"` 默认值（向后兼容 manifest.json）。
  - **F10（P2）** chapters/chapterPages 固定 `Semaphore(3)` 无间隔，与服务端 ~2 req/s 冲突 → 触发全局 20s 冷却，体验为「长漫画偶发整体卡 20 秒」。建议收敛到单点 PicaRateLimiter（参数需真机压测）。
  - P3：persistHost 每请求落盘、switchHost/baseUrl 可见性与锁、手工 Content-Length + 重定向掩盖 401、死代码（`ImageDetail.proxyUrl` / `ui/theme/ImageUrl.kt:imageUrl()` / `PicaMethod` / `ImageQuality` / `PicaHttpEngine.headers` / execute 与 executeAsync 二选一）、注册生日边界、凭据默认长期保存+allowBackup、分类缓存无失效入口。
- 已核实为「不建议动」：注释里的 `-keep class com.pika.core.**`（M2 计划，开启会显著增包体，需独立评估）、密钥迁 NDK（独立工程，Rewrite Requires Approval 级）、PicaHttpEngine 的 doOutput/手写 outputStream（当前唯一能过 Cloudflare 的组合，别顺手换回 OkHttp）、SourcePrefs 的 runBlocking 兜底（与既定范式一致，可接受）。
- Gate：`Local Fix` + `Local Fix Only`，分 5 批（批1=F1+F6；批2=F3+F11+F12；批3=F7+F8；批4=F2+F9；批5=F10+其余P3），F4 独立出方案。
- 验证缺口：仓库 `src/test`、`androidTest` 全空，零自动化测试；建议优先给 `picaSignature()` / `ImageDetail.directUrl` / `rankQuery()` / `PicaException.isRateLimit` 这四个无 Android 依赖的纯函数补 JVM 单测。

## 第二部分评审（只读）：除禁漫源外的全部应用功能
- 覆盖对账：全仓 75 个 Kotlin 文件 / 14,265 行；排除 4 个禁漫源实现（JmcomicSource/JmClient/JmModels/JmCrypto，770 行）。第一部分 13 文件 + 本轮 58 文件 = **71/75 = 94.7%**。
- 方法：三路分片代理（Explore 子代理）并行精读 + 主评审精读基础设施与数据层，并**回源码逐行复核所有 P1**。
- 产出：`reports/app-review-20260912.html`（33 条：P1×5 / P2×17 / P3×17）。
- **两处修正了分片结论**（重要，避免误判）：
  - 「评论区永久卡死」由 P1 下调 P2：`ComicDetailScreen` 用 `viewModel()` 绑定 NavBackStackEntry，漫画路由参数不同时不复用栈顶 → 同一 VM 实例不会被以不同 comicId 调用，触发路径**大概率不可达**，仅作防御性修复。新增验证项 V12。
  - 「多词批量拉取自触限流」重新定界：并发 4/3 均有作者实测依据（`HomeViewModel.kt:389` 注释「实测并发 4 安全」），**不据推算判错**；真正缺陷是**缺冷却熔断**。→ 修复方式定为「补熔断+补节拍，不动已调好的并发度」。

## 全面修复（本次落地，45/50 项）
- 新增 `reports/fix-verification-20260912.html`。改动：**45 个 Kotlin 文件 + 新增 `core/CoroutineExt.kt` + 删除 `ui/theme/ImageUrl.kt`**。
- **F4 密钥加固拿到「改前/改后」二进制对照实证**：扫描 `pika-v1.5.47.apk`（改前）→ 完整明文 API_KEY/SECRET_KEY **与两段片段全部 FOUND**；改后 `app-release.apk` → 全部 absent。这**证实**了原注释「避免完整密钥出现在常量池」在 `const val` 拼接下完全无效。修法：`PicaConfig` 改为运行时按位还原（`intArrayOf` + `xor KEY_MASK`），`API_KEY`/`SECRET_KEY` 由 `const val` 变 `val get()`。
- 关键实现取舍（后续维护必读）：
  - **F2**：抽出 `private withRetry(block, requireData)`，公开 `safeCall`（要求 data）与 `safeCallUnit`（只校验 code）；`code != 200` 校验保持不变，只放开 7 个无返回体写接口。注意 `safeCallUnit<T>(block: suspend () -> ApiResponse<T>)` 必须用泛型参数，用 `ApiResponse<*>` 会因星投影无法推断 T。
  - **F24**：不采纳「给排行榜加延迟」（首屏默认落在排行榜，会空等）→ 改为**延后不可见的关注流**（仅当首屏在排行榜时 delay 1200ms），零感知延迟。
  - **F13**：**曾一度按报告建议关闭自动跟随重定向，复核时判定过激并回退** —— 若宿主域名有正常跳转会导致全部请求失败，属未验证的行为变更。改为「保留跟随 + 最终 URL 与请求 URL 不一致时记告警」。
  - **F44 回退**：`AuthorScreen` 的 `unsupported` 语义是「不支持按作者浏览作品」，**不是** `supportsStatusFilter`（完结字段）。照报告改会造成行为错误。要改需在 `Source` 接口新增 `supportsAuthorBrowse`，会触及禁漫源，超出范围。已在代码留下说明注释。
  - **F30**：新增 `core/CoroutineExt.kt` 的 `runCatchingCancellable`（重抛 CancellationException），落地 20+ 处；替代 `runCatching` 时可保留「普通失败继续、取消放行」的原有语义。
  - **F19**：`snapshotFlow` 的 State 读取**必须写在 lambda 内部**才产生订阅；另加 `DisposableEffect{onDispose}` + `rememberUpdatedState` 兜底（单 Activity + Navigation 下点返回不触发 Activity ON_STOP）。
  - **F34**：`WebtoonSplitPage` 的 `heightRatio` 原用 `remember(pageIndex, safeSliceCount)`，切片数从 1 变 n 会把实测比例重置为估算值 → 改用 `measuredRatio`（key 只含 pageIndex）。
  - **F29**：`ReaderPrefs` 补 `loadCache()` 后台预热（对齐 `SourcePrefs` 范式）。`SourcePrefs.setActiveSource` 被 `markActiveSource`（内存优先+后台落盘）取代后删除。
  - **F16**：`SecureAccountStore.saveEnabled` 默认 **true**（保持既有「一键重登」不被静默改变），登录页可取消勾选；`hasSaved()` 随之删除。
- **构建环境坑（重要，下次直接用）**：Bash 工具在本沙箱**不可用**（shim 的 `dirname` 找不到 → PATH 未建立 → 无 grep/ls/head；且 `wsl.exe` 在安全策略黑名单里，`gbuild.sh` 无法执行）。**改用 PowerShell 调 `.\gradlew.bat`**（需 `$env:JAVA_HOME="C:\Program Files\Android\Android Studio\jbr"`，并 `dangerouslyDisableSandbox`，先删 `~/.gradle/native` 与 `~/.gradle/daemon`）。另：**PowerShell 工具在此环境不回显 stdout**，必须把结果 `Set-Content` 写文件再用 Read 读取；直接把 `-Dorg.gradle.appname=gradle` 写在命令行会被 PS 吞掉参数，必须用 `gradlew.bat` 或放进数组。
- 验证结论：`compileReleaseKotlin` 与 `clean assembleRelease`（含 R8）均 BUILD SUCCESSFUL；产物 6.19 MB；密钥扫描通过。**未做真机运行**（无测试、未抓包、未压测）。

## 仓库遗留状态（非本次改动，需用户确认）
- `git status` 显示约 40 项 **会话开始前就已存在**的未提交删除：`pika-v1.5.12.apk`~`v1.5.46.apk`、`apk/pika-v0.3.2.apk`、`release.sh`、`jm-api-verification.html`、`jm-source-plan.html`、`pika-updatedat-feasibility.html`、`pika代码评审报告*.md`，以及 `M .workbuddy/memory/2026-09-04.md`。会话初始项目快照即无这些文件，**与本次修复无关**，勿误报为本次改动。

## 第二部分：除禁漫源外的全应用功能评审（只读，未改源码）
- 范围扩展：四主 Tab、详情/阅读器/下载/关注/历史/评论/作者、账号体系、设置、更新链路、全部持久化封装、导航与主题/打包配置。
- 方法：三路 Explore 分片评审（设置与账号 UI / 浏览搜索首页 / 阅读器详情下载）+ 主评审精读核心基础设施与数据层，**所有 P1 均回源码逐行复核**。
- 产出：`reports/app-review-20260912.html`（F18–F50，31 条，P1×5 / P2×17 / P3×17 + 验证缺口 V7–V13 + 合并批次计划含批次 0）。
- **覆盖对账**：全仓 75 个 .kt / 14,265 行；排除 4 个禁漫源实现（JmcomicSource/JmClient/JmModels/JmCrypto = 770 行）；第一部分 13 个 + 本轮 58 个 = **71/75 = 94.7%**。
- P1（5 条，均已逐行确认）：
  - **F18** `UpdateDialog.kt:43` 读 `cacheDir`，`UpdateManager.kt:173` 写 `filesDir` → 从**首页横幅**更新时点「立即安装」永远静默关窗、装不上（`apkFile.exists()` 恒 false）。对照证据：`SettingsScreen.kt:343-347` 用 `downloadAndVerify` 返回值是对的 → 更新有两套实现，只坏了一套。`UpdateDialog` 全项目仅 `HomeScreen.kt:117` 一处调用。
  - **F19** `ReaderScreen.kt:253-263` `snapshotFlow { currentPage }` 读的是组合期普通 val（State 读取在块外），**只发射一次**；且 `LaunchedEffect(pages.size, scrollMode)` 本章内不重启；退出阅读器无任何兜底（`:131-154` 两个 DisposableEffect 只管系统栏/屏幕常亮）。全仓 saveProgress 仅 `:259` 与 ON_STOP `:265` 两处 → 单 Activity 下点返回不触发 ON_STOP → **本章内进度基本不落盘，续读退回章首**。修法：状态读进 flow 块 + onDispose 补写。
  - **F20** `LoginScreen.kt:90-96` 密码框缺 `visualTransformation`，且 `:50-58` 自动回填 Keystore 解出的明文密码 → 打开登录页即明文可见。`RegisterScreen.kt:152` 已有正确写法。
  - **F21** `LogScreen.kt:109` `key = "${timestamp}_${tag}_${level}"`，LogStore 时间戳为 currentTimeMillis → 同毫秒+同 tag+同 level 即重复 key，LazyColumn 抛 IllegalArgumentException 硬崩。触发：章节 fan-out 3 路并发同时失败（PicaClient 同 tag/level 日志）。修法：LogEntry 加自增 id。
  - **F22** `SearchViewModel.kt:222-307` / `HomeViewModel.kt:394-419` 多页批量拉取**无 `rateLimitRemaining()` 熔断**（并发 4 / 间隔 250ms / 上限 50 页），而 `HomeViewModel.kt:326-330` 的逐来源循环有 —— 项目内标准不一致。**注意：并发 4 的数值有作者实测依据（`:389` 注释），不要据此判错、不要先调参，应先补熔断**（需 V10 压测）。
- P2 重点：F23 切源与跳登录竞态（`SourceManager.switch` 先落盘后改内存，导航先跑 → 凭据可能提交给旧源）；F24 冷启动 `refresh()` 与 `loadRank(0)` 同帧并发自撞限流（`HomeScreen.kt:68` 默认 tab=1 与 `:42` 注释矛盾）；F25 排行榜/收藏翻页无代际守卫（`BrowseViewModel` 的 loadToken、`ComicDetailViewModel` 的 loadGeneration 已做对，这三处漏网）；F26 搜索 `finally` 无 `isActive` 守卫（`HomeViewModel.kt:278` 已做对）+ resetAll 不取消不清 `_activeSearchWords` + loadMore 吞取消误置 endReached；F27 评论区 `_commentLoading` 可能永久卡死（`load()` 清理清单漏它）—— **本轮未能构造触发路径，已从 P1 下调 P2**（VM 按 NavBackStackEntry 隔离，每个 comicId 独立实例）；F28 收藏态未初始化首点语义相反（`Comic.pagesCount` 附近已有 `isFavourite` 字段未透传）；F29 `ReaderPrefs` 未做启动预热（`SourcePrefs` 有 `loadCache`，ReaderPrefs 没有）→ `readerMode`/`brightness` getter 主线程 runBlocking 且在组合期调用；F30 CancellationException 被吞（跨 10+ 文件，项目已有多处正确范式）；F31 首页滚动位置固定取 followGridState；F32 章节列表组合期遍历目录；F33 预取无 size 上限+显式 memoryCacheKey 绕过解码上限；F34 长图切片比例回退估算值；F35 滚动→翻页页码不同步；F36 头像全尺寸解码 OOM；F37 关注列表 key 拼接歧义可崩。
- P3 要点：F38 更新逻辑两套实现重复（F18 的成因）；F39 死代码（`UpdateState.checkManual` / `ReaderViewModel.clear()` 注释说退出清理其实没人调 / `AuthorViewModel.loadMore` / `HomeScreen` followEndReached 收集但硬编码 / `BrowseViewModel.setStatus`+分类状态筛选链空转）；F40 collectAsState 未用 lifecycle 版；F41 组合期同步读 SharedPreferences；F42 CategoryReorderDialog items 无 key + remember 未以 categories 为 key、DownloadScreen expandedComic remember 在分支内；F44 AuthorScreen/MineScreen 硬编码 SourceType.JMCOMIC（应用能力位）；F46 ProfileScreen error 字段混用成功/失败靠 contains 判断；F47 UpdateState 用类型转换访问自己的 StateFlow。
- 两部分交叉点（修复时必须一起看，F18 就是「改一边漏一边」的现成样本）：F10↔F22（限流保护）、F16↔F20（密码存储与显示）。
- 批次计划新增**批次 0**（F18/F19/F20/F21，4-5 文件 &lt;40 行，零外部依赖，可立即做）；原批 1-5 相应后移，F22 与第一部分 F10 建议合并为「统一节流器」改造。
